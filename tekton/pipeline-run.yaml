apiVersion: tekton.dev/v1
kind: PipelineRun

metadata:
  generateName: tax-calculator-run-

spec:
  pipelineRef:
    name: tax-calculator-pipeline

  params:
    - name: IMAGE
      value: us.icr.io/sn-labs-daniachaudh/tax-calculator

    - name: TAG
      value: pipeline-1

  workspaces:
    - name: source
      persistentVolumeClaim:
        claimName: tax-calculator-source
